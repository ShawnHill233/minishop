<tr>
  <td><%= category.name %></td>
  <td><%= category.parent.name if category.parent.present? %></td>
  <td>
    <%= link_to '编辑', edit_admin_category_path(category) %>
    <%= link_to '删除', admin_category_path(category), method: :delete %>
  </td>
</tr>