<div class="nested-fields">
  <label>属性名</label>
  <%= f.select :property_name, options_from_collection_for_select(Property.all, :name, :name, f.object.property_name) %>

  <label>属性值</label>
  <%= f.text_field :value %>

  <%= link_to_remove_association "移除属性", f %>
</div>
